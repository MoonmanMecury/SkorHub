## 2026-04-18 - Keyboard Navigation for Search Results
**Learning:** For accessible search dropdowns, implementing the combobox pattern with `aria-activedescendant` and `useId` provides a seamless screen reader experience without moving physical focus away from the input.
**Action:** Always use `useId` for stable ARIA relationships and ensure visual highlights match `aria-selected` states.
