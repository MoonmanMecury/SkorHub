## 2026-05-20 - Improved RedirectAlert UX
**Learning:** Using browser-native `alert()` for redirects is jarring and breaks the app's immersive feel, especially in a dark-themed, high-polish application. A custom countdown UI provides better feedback and control to the user.
**Action:** Always prefer non-blocking, themed UI components for redirection notices and state changes over native browser dialogs.
