## 2026-05-04 - [Non-blocking System Alerts]
**Learning:** Using browser-native `alert()` blocks the main thread and feels disjointed from the application's design system. A custom, themed redirection card with a countdown provides better context and a smoother transition.
**Action:** Replace native blocking alerts with themed UI components that include progress indicators (countdowns/spinners) and manual overrides (buttons).
